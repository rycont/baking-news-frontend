import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
    appId: 'app.postica.bakingnews',
    appName: 'Baking News',
    webDir: 'dist',
    server: {
        androidScheme: 'https',
    },
}

export default config
